import logging
from datetime import datetime, timedelta
from database import AsyncSessionLocal, start_program_for_user, mark_program_day_completed, get_user_program_progress
from sqlalchemy import text

logger = logging.getLogger(__name__)

class ProgramManager:
    def __init__(self, bot):
        self.bot = bot
        self.program_content = {
            1: {
                "title": "День 1: Осознание настоящего",
                "message": """🌱 *День 1: Осознание настоящего*

Сегодня мы начинаем с простого, но очень важного шага - осознания текущего момента.

*Ваше задание на сегодня:*
1. Утром, после пробуждения, найдите 2 минуты тишины
2. Сядьте удобно и просто понаблюдайте за своим дыханием
3. Не пытайтесь его изменить - просто заметьте, как воздух входит и выходит

*Почему это важно:*
Это упражнение помогает "проснуться" нервной системе и настроить ее на спокойный режим работы.""",
                "reminder": "Напоминание: найдите 2 минуты для осознанного дыхания сегодня утром"
            },
            2: {
                "title": "День 2: Водный баланс", 
                "message": """🌱 *День 2: Водный баланс*

Вода - основа всех процессов в организме, включая гормональный баланс.

*Ваше задание на сегодня:*
1. Выпейте стакан теплой воды сразу после пробуждения
2. В течение дня пейте воду небольшими глотками
3. Попробуйте добавить в воду дольку лимона или немного мяты

*Почему это важно:*
Достаточная гидратация улучшает метаболизм и помогает выводить токсины.""",
                "reminder": "Не забудьте про стакан воды утром и в течение дня!"
            },
            3: {
                "title": "День 3: Цифровой детокс",
                "message": """🌱 *День 3: Цифровой детокс*

Синий свет экранов может нарушать циркадные ритмы и мешать качественному сну.

*Ваше задание на сегодня:*
1. За 1 час до сна отложите все гаджеты
2. Вместо этого почитайте книгу или послушайте спокойную музыку
3. Подготовьте спальню ко сну: приглушите свет, проветрите комнату

*Почему это важно:*
Это помогает естественной выработке мелатонина - гормона сна.""",
                "reminder": "Вечерний цифровой детокс: за час до сна отложите гаджеты"
            },
            4: {
                "title": "День 4: Осознанное питание",
                "message": """🌱 *День 4: Осознанное питание*

Как мы едим так же важно, как и что мы едим.

*Ваше задание на сегодня:*
1. Выберите один прием пищи для практики осознанности
2. Ешьте медленно, тщательно пережевывая пищу
3. Обращайте внимание на вкус, текстуру и запах еды

*Почему это важно:*
Осознанное питание улучшает пищеварение и помогает распознавать настоящий голод.""",
                "reminder": "Попробуйте осознанный прием пищи сегодня"
            },
            5: {
                "title": "День 5: Двигательная активность",
                "message": """🌱 *День 5: Двигательная активность*

Движение - естественное состояние нашего тела.

*Ваше задание на сегодня:*
1. Найдите 10-15 минут для легкой физической активности
2. Это может быть прогулка, stretching или простые упражнения
3. Слушайте свое тело - не перенапрягайтесь

*Почему это важно:*
Регулярное движение улучшает кровообращение и снижает уровень стресса.""",
                "reminder": "Не забудьте про легкую физическую активность сегодня"
            },
            6: {
                "title": "День 6: Практика благодарности",
                "message": """🌱 *День 6: Практика благодарности*

Фокус на позитивном меняет химию мозга.

*Ваше задание на сегодня:*
1. Вечером запишите 3 вещи, за которые вы благодарны сегодня
2. Это могут быть простые моменты: вкусный кофе, улыбка прохожего, хорошая погода
3. Постарайтесь почувствовать благодарность, а не просто записать

*Почему это важно:*
Практика благодарности снижает уровень кортизола и улучшает настроение.""",
                "reminder": "Вечерняя практика: 3 вещи, за которые вы благодарны"
            },
            7: {
                "title": "День 7: Анализ первой недели",
                "message": """🌱 *День 7: Анализ первой недели*

Первая неделя завершена! Давайте подведем итоги.

*Ваше задание на сегодня:*
1. Вспомните, какие практики вам понравились больше всего
2. Отметьте, что изменилось в вашем самочувствии
3. Решите, какие привычки вы хотите сохранить

*Почему это важно:*
Рефлексия помогает закрепить результаты и настроиться на следующую неделю.""",
                "reminder": "Неделя завершена! Подведите итоги и отпразднуйте успехи"
            },
            # Дни 8-14 продолжают углублять практики
            14: {
                "title": "День 14: Завершение и новые начала",
                "message": """🎉 *День 14: Завершение и новые начала*

Поздравляем! Вы завершили 14-дневную программу восстановления.

*Ваши достижения:*
• Вы создали основу для здоровых привычек
• Научились слушать свое тело
• Поняли, что небольшие ежедневные действия имеют большое значение

*Что дальше:*
Продолжайте практиковать то, что вам подошло. Рекомендуем повторить анализ через 3 месяца, чтобы увидеть прогресс.

Спасибо, что прошли этот путь с нами! 🌟""",
                "reminder": "🎉 Поздравляем с завершением 14-дневной программы!"
            }
        }
    
    async def start_program(self, user_id: int):
        """Запускает 14-дневную программу для пользователя"""
        try:
            day_number = await start_program_for_user(user_id)
            if day_number:
                await self.send_day_message(user_id, day_number)
                logger.info(f"🌱 14-дневная программа запущена для пользователя {user_id}, день {day_number}")
                return True
            return False
        except Exception as e:
            logger.error(f"❌ Ошибка запуска программы для пользователя {user_id}: {e}")
            return False

    async def send_day_message(self, user_id: int, day: int):
        """Отправляет сообщение дня программы"""
        try:
            from database import get_user_by_tg_id
            from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton
            
            user = await get_user_by_tg_id(user_id)
            if not user or day not in self.program_content:
                return False
            
            content = self.program_content[day]
            message = content["message"]
            
            # Создаем клавиатуру с кнопками действий
            keyboard = InlineKeyboardMarkup(inline_keyboard=[
                [InlineKeyboardButton(text="✅ Сделано", callback_data=f"program_done:{day}")],
                [InlineKeyboardButton(text="⏭️ Пропустить", callback_data=f"program_skip:{day}")],
                [InlineKeyboardButton(text="📊 Мой прогресс", callback_data="program_progress")]
            ])
            
            await self.bot.send_message(
                user.tg_id,
                message,
                parse_mode="Markdown",
                reply_markup=keyboard
            )
            
            # Планируем напоминание на вечер
            if "reminder" in content:
                from notifications import NotificationManager
                notification_manager = NotificationManager(self.bot)
                
                # Напоминание через 8 часов
                reminder_time = datetime.utcnow() + timedelta(hours=8)
                await notification_manager.send_notification(
                    user_id,
                    f"🌱 {content['reminder']}"
                )
            
            logger.info(f"✅ Сообщение дня {day} отправлено пользователю {user_id}")
            return True
            
        except Exception as e:
            logger.error(f"❌ Ошибка отправки сообщения дня {day} пользователю {user_id}: {e}")
            return False

    async def mark_day_completed(self, user_id: int, day: int):
        """Отмечает день программы как выполненный"""
        try:
            success = await mark_program_day_completed(user_id, day)
            if success:
                # Отправляем подтверждение
                from database import get_user_by_tg_id
                user = await get_user_by_tg_id(user_id)
                if user:
                    if day == 14:
                        completion_message = """🎉 *Поздравляем с завершением программы!*

Вы прошли 14-дневный путь к лучшему самочувствию. Ваши результаты:
• Улучшилось понимание потребностей тела
• Сформировались полезные привычки
• Появились инструменты для управления энергией

Рекомендуем повторить анализ через 3 месяца для оценки прогресса."""
                    else:
                        completion_message = f"✅ *День {day} завершен!* Отличная работа! Продолжаем завтра."
                    
                    await self.bot.send_message(
                        user.tg_id,
                        completion_message,
                        parse_mode="Markdown"
                    )
                
                logger.info(f"✅ День {day} отмечен как выполненный для пользователя {user_id}")
                return True
            return False
        except Exception as e:
            logger.error(f"❌ Ошибка отметки дня {day} как выполненного: {e}")
            return False

    async def mark_day_skipped(self, user_id: int, day: int):
        """Отмечает день программы как пропущенный"""
        try:
            async with AsyncSessionLocal() as session:
                # Находим запись прогресса
                result = await session.execute(
                    text("""
                        SELECT * FROM program_progress 
                        WHERE user_id = :user_id AND day_number = :day_number
                    """),
                    {"user_id": user_id, "day_number": day}
                )
                progress_data = result.fetchone()
                
                if progress_data:
                    # Обновляем запись
                    await session.execute(
                        text("""
                            UPDATE program_progress 
                            SET skipped = true 
                            WHERE user_id = :user_id AND day_number = :day_number
                        """),
                        {"user_id": user_id, "day_number": day}
                    )
                    
                    # Создаем запись для следующего дня
                    if day < 14:
                        next_day_exists = await session.execute(
                            text("""
                                SELECT 1 FROM program_progress 
                                WHERE user_id = :user_id AND day_number = :next_day
                            """),
                            {"user_id": user_id, "next_day": day + 1}
                        )
                        
                        if not next_day_exists.fetchone():
                            await session.execute(
                                text("""
                                    INSERT INTO program_progress (user_id, day_number, completed, skipped)
                                    VALUES (:user_id, :day_number, false, false)
                                """),
                                {"user_id": user_id, "day_number": day + 1}
                            )
                    
                    await session.commit()
                    
                    # Отправляем сообщение о пропуске
                    from database import get_user_by_tg_id
                    user = await get_user_by_tg_id(user_id)
                    if user:
                        await self.bot.send_message(
                            user.tg_id,
                            f"⏭️ День {day} пропущен. Не беспокойтесь, продолжаем с дня {day + 1}!",
                            parse_mode="Markdown"
                        )
                    
                    logger.info(f"⏭️ День {day} пропущен пользователем {user_id}")
                    return True
            return False
        except Exception as e:
            logger.error(f"❌ Ошибка отметки дня {day} как пропущенного: {e}")
            return False

    async def get_user_progress(self, user_id: int):
        """Получает прогресс пользователя в программе"""
        try:
            progress_data = await get_user_program_progress(user_id)
            
            completed_days = [p.day_number for p in progress_data if p.completed]
            skipped_days = [p.day_number for p in progress_data if p.skipped]
            current_day = max([p.day_number for p in progress_data] or [0])
            
            progress = {
                "completed_days": completed_days,
                "skipped_days": skipped_days,
                "current_day": current_day,
                "total_days": 14,
                "completion_rate": len(completed_days) / 14 * 100 if completed_days else 0
            }
            
            return progress
        except Exception as e:
            logger.error(f"❌ Ошибка получения прогресса пользователя {user_id}: {e}")
            return None

    async def send_progress_report(self, user_id: int):
        """Отправляет отчет о прогрессе пользователю"""
        try:
            progress = await self.get_user_progress(user_id)
            if not progress:
                return False
            
            from database import get_user_by_tg_id
            user = await get_user_by_tg_id(user_id)
            if not user:
                return False
            
            completed_count = len(progress["completed_days"])
            skipped_count = len(progress["skipped_days"])
            current_day = progress["current_day"]
            completion_rate = progress["completion_rate"]
            
            message = f"""📊 *Ваш прогресс в программе*

*Текущий день:* {current_day}/14
*Завершено дней:* {completed_count}
*Пропущено дней:* {skipped_count}
*Общий прогресс:* {completion_rate:.1f}%

{"🎉 Отличные результаты! Продолжайте в том же духе!" if completion_rate > 70 else "💪 Хороший старт! Каждый день приближает к цели!" if completion_rate > 30 else "🌟 Начало положено! Помните, что регулярность - ключ к успеху."}"""
            
            await self.bot.send_message(
                user.tg_id,
                message,
                parse_mode="Markdown"
            )
            
            logger.info(f"📊 Отчет о прогрессе отправлен пользователю {user_id}")
            return True
            
        except Exception as e:
            logger.error(f"❌ Ошибка отправки отчета о прогрессе пользователю {user_id}: {e}")
            return False

    async def send_program_offer(self, user_id: int):
        """Отправляет предложение начать программу"""
        try:
            from database import get_user_by_tg_id
            from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton
            
            user = await get_user_by_tg_id(user_id)
            if not user:
                return False
            
            keyboard = InlineKeyboardMarkup(inline_keyboard=[
                [InlineKeyboardButton(text="🌱 Начать программу", callback_data="start_program")],
                [InlineKeyboardButton(text="📊 Узнать подробнее", callback_data="program_info")],
                [InlineKeyboardButton(text="🕐 Не сейчас", callback_data="program_later")]
            ])
            
            message = """🌱 *14-дневная программа восстановления*

Готовы сделать следующий шаг к лучшему самочувствию?

*Что вас ждет:*
• Ежедневные простые практики
• Персональные рекомендации
• Поддержка на каждом этапе
• Измеримые результаты

*Программа включает:*
- Практики осознанности
- Рекомендации по питанию
- Двигательную активность
- Работу со сном и стрессом

Начните свой путь к сбалансированной энергии уже сегодня!"""
            
            await self.bot.send_message(
                user.tg_id,
                message,
                parse_mode="Markdown",
                reply_markup=keyboard
            )
            
            logger.info(f"🌱 Предложение программы отправлено пользователю {user_id}")
            return True
            
        except Exception as e:
            logger.error(f"❌ Ошибка отправки предложения программы пользователю {user_id}: {e}")
            return False

    async def send_program_info(self, user_id: int):
        """Отправляет подробную информацию о программе"""
        try:
            from database import get_user_by_tg_id
            from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton
            
            user = await get_user_by_tg_id(user_id)
            if not user:
                return False
            
            keyboard = InlineKeyboardMarkup(inline_keyboard=[
                [InlineKeyboardButton(text="🌱 Начать программу", callback_data="start_program")],
                [InlineKeyboardButton(text="🔙 Назад", callback_data="back_to_offer")]
            ])
            
            message = """🌱 *Подробнее о 14-дневной программе*

*Как это работает:*
Каждый день в течение 14 дней вы будете получать:
1. *Утреннее сообщение* с заданием дня
2. *Теоретическую часть* - почему это важно для вашего здоровья
3. *Практическое задание* - конкретные действия на день
4. *Вечернее напоминание* - чтобы не забыть о практике

*Темы программы:*
1-3 дни: Основа - осознанность, гидратация, цифровой детокс
4-7 дни: Питание и движение - осознанное питание, физическая активность
8-10 дни: Сон и восстановление - ритуалы перед сном, управление стрессом
11-14 дни: Интеграция - закрепление привычек, планирование поддержания результатов

*Результаты программы:*
• Улучшение качества сна
• Повышение уровня энергии
• Снижение уровня стресса
• Лучшее понимание потребностей тела
• Набор инструментов для поддержания баланса

Программа разработана с учетом современных исследований в области нутрициологии, психологии и хронобиологии."""
            
            await self.bot.send_message(
                user.tg_id,
                message,
                parse_mode="Markdown",
                reply_markup=keyboard
            )
            
            logger.info(f"🌱 Информация о программе отправлена пользователю {user_id}")
            return True
            
        except Exception as e:
            logger.error(f"❌ Ошибка отправки информации о программе пользователю {user_id}: {e}")
            return False
